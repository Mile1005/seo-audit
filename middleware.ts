// Middleware temporarily disabled to fix Google OAuth invalid_grant error
// The canonical host enforcement was interfering with OAuth callback flow
// TODO: Re-implement after OAuth is stable, with proper auth route exclusions
